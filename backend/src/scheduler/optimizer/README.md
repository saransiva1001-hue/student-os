# Scheduler Optimizer Directory

Contains constraint satisfaction problem (CSP) solvers, genetic algorithm solvers, and heuristics solvers.
- Finds optimal schedule configurations that satisfy all calendar constraints while maximizing productivity scores.
