# Database Documentation

This folder houses entity schemas, migrations logs, and ORM config guidelines.

## SQLite Database
- Saved locally at `backend/database/dev.db`.
- Managed using **Prisma ORM**.
